/*
 * ------------------------------------------
 * 需要平台适配的接口实现文件
 * @version  1.0
 * @author   genify(caijf@corp.netease.com)
 * ------------------------------------------
 */
NEJ.define(function(p){
	/**
	 * 关联file的label点击事件
	 * @return {Void}
	 */
	p.__handleFileLabelClick = function(){
		//do nothing
	};

	return p;
});